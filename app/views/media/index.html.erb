<div class="container my-5 text-light p-5 rounded shadow" id="media-index-container">
  <h2 class="mb-4 text-center">Search for a movie <i class="fa-solid fa-magnifying-glass"></i></h2>

  <div data-controller="media-search">

    <%= form_with url: search_media_path,
          method: :post,
          data: {
            action: "submit->media-search#submit", turbo: false
           },
          class: "input-group shadow-sm" do %>

    <%= text_field_tag :title, nil,
          class: "form-control form-control-lg",
          placeholder: "Enter a media title...",
          required: true %>

    <%= hidden_field_tag :medium_type, "",
      data: { media_search_target: "mediumType" } %>

      <button class="btn btn-primary btn-lg ms-2" type="submit">
        <i class="bi bi-search"></i> Search
      </button>

    <% end %>

    <!-- Boutons (même controller, cachés au départ) -->
    <div class="mt-4 d-none"
        data-media-search-target="buttons">

      <button class="btn btn-outline-primary btn-lg me-2"
        data-action="click->media-search#choose"
        data-medium="movie">
        🎬 Movies
      </button>

      <button class="btn btn-outline-success btn-lg me-2"
        data-action="click->media-search#choose"
        data-medium="book">
        📚 Books
      </button>

      <button class="btn btn-outline-warning btn-lg"
        data-action="click->media-search#choose"
        data-medium="game">
        🎮 Video Games
      </button>
    </div>

  </div>

</div>
