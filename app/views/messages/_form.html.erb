  <%= simple_form_for [chat, message] do |f| %>
    <%= f.input :content, label: "Ask a question", required: true, placeholder: "What movie should I watch next?" %>
    <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
  <% end %>
